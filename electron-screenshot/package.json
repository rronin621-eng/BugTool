{
  "name": "bug-screenshot-tool",
  "version": "1.0.0",
  "description": "BUG截图工具 - 快捷键截图+标注+录入BUG",
  "main": "dist/main/index.js",
  "scripts": {
    "copy-renderer": "mkdir -p dist/renderer/viewer dist/assets && cp src/renderer/index.html src/renderer/screenshot.js src/renderer/style.css src/renderer/img-preview.html dist/renderer/ && cp src/renderer/viewer/index.html src/renderer/viewer/viewer.js src/renderer/viewer/viewer.css dist/renderer/viewer/ && cp src/assets/tray-icon.png src/assets/tray-icon@2x.png dist/assets/",
    "dev": "tsc && npm run copy-renderer && electron .",
    "build": "tsc && npm run copy-renderer",
    "start": "electron ."
  },
  "dependencies": {
    "axios": "^1.6.0",
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
